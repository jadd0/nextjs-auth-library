export * from './server/index.js'

/** Used in a user's "auth.ts" or corresponding file to assist them with authentication setup */
export type { AuthConfig } from './shared/validation/server/config.js'